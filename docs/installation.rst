.. highlight:: shell

============
Installation
============


Stable release
--------------

To install cpy_amm, run this command in your terminal:

.. code-block:: console

    $ pip install cpy_amm

This is the preferred method to install cpy_amm, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for cpy_amm can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/lehajam/cpy_amm

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/lehajam/cpy_amm/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/lehajam/cpy_amm
.. _tarball: https://github.com/lehajam/cpy_amm/tarball/master
